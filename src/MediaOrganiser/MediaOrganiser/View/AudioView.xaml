<UserControl x:Class="MediaOrganiser.View.AudioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaOrganiser.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:MediaOrganiser.ViewModel">

    <Control.DataContext><vm:AudioViewModel /></Control.DataContext>

    <DockPanel LastChildFill="True">
        <Label DockPanel.Dock="Bottom" Margin="5" Content="{Binding CountText}"></Label>
        <!--<StackPanel Visibility="{Binding DetailsPanelVisible}" DockPanel.Dock="Left">
            <Label Content="{Binding SelectedFile.Title}"></Label>
        </StackPanel>-->
            <DataGrid SelectionMode="Single" SelectedItem="{Binding Path=SelectedFile, Mode=TwoWay}" ItemsSource="{Binding Files}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Artist" Binding="{Binding Artist}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
    </DockPanel>

</UserControl>
